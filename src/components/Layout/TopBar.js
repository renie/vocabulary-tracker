import SearchBar from '../SearchBar'

import './topBar.css'

const TopBar = () => {
    return (
        <div className="top-bar">
            <SearchBar />
        </div>
    )
}

export default TopBar
